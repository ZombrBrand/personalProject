{
  "name": "webpack-demo",
  "version": "1.0.0",
  "description": "// 随时随地将你的当前目录变成一个静态文件服务器的根目录。\r     cnpm i -g anywhere",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:babel && npm run build:watch",
    "build:babel": "babel src -d build",
    "build:watch": "babel --watch src -d build",
    "build:server": "browser-sync start --server -f build/*.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "browser-sync": "^2.18.12",
    "browserify": "^14.3.0",
    "less": "^2.7.2",
    "less-plugin-clean-css": "^1.5.1"
  }
}
